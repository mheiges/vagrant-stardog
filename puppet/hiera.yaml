---
:backends:
    - yaml
    
:yaml:
    :datadir: "%{settings::codedir}/environments/%{::environment}/hieradata"

:hierarchy:
    - "nodes/%{::fqdn}"
    - "%{::domain}"
    - sensitive
    - common
